import { RouteGuardService } from './route-guard-service';

describe('RouteGuardService', () => {
  it('should create an instance', () => {
    expect(new RouteGuardService()).toBeTruthy();
  });
});
